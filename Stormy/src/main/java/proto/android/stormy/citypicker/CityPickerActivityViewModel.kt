package proto.android.stormy.citypicker

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class CityPickerActivityViewModel(application: Application) : AndroidViewModel(application)